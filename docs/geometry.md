📘 Cycloidal Gear Geometry

A practical engineering guide for understanding and using cycloidal gear geometry in this MATLAB toolkit.

1. Introduction

Cycloidal gears are defined by the rolling motion of a circle (the rolling circle) along another circle (the pitch circle). They were historically used in clocks and precision mechanisms because they offer:

- smooth meshing

- low friction

- low wear

- tolerance of small manufacturing errors

- excellent performance at low torque

This document explains the geometry implemented in CBT_CycloidGear and how the parameters relate to the mathematical curves.

2. The Pitch Circle
3. 
Every cycloidal gear begins with a pitch circle of radius:

𝑅
𝑝
=
𝑚
⋅
𝑁
2
𝜋
where:

𝑚
 — module

𝑁
 — tooth count

The pitch circle is the reference for:

- tooth spacing

- rolling circle motion

- addendum and dedendum heights

- fillet geometry

- meshing between pinion and wheel

In this toolkit, the pitch radius is stored as:

- gear.radiusPitch

3. Rolling Circle

Cycloidal gears use a rolling circle of radius:

𝑅
𝑟
=
𝑚
2
This is the standard choice for clockmaking and matches the classical cycloid definition.

The rolling circle generates:

epicycloid (addendum flank)

hypocycloid (dedendum flank)

In the code, this is:

Code
gear.rhoA   % addendum rolling radius
gear.rhoD   % dedendum rolling radius
You allow independent values for addendum and dedendum radii, which is a powerful generalisation.

4. Tooth Flank Geometry
Each tooth consists of two curve segments:

4.1 Epicycloid (Addendum)
Generated by rolling the circle outside the pitch circle.

Parametric form:

𝑥
(
𝜃
)
=
(
𝑅
𝑝
+
𝑅
𝑟
)
cos
⁡
𝜃
−
𝑅
𝑟
cos
⁡
(
𝑅
𝑝
+
𝑅
𝑟
𝑅
𝑟
𝜃
)
𝑦
(
𝜃
)
=
(
𝑅
𝑝
+
𝑅
𝑟
)
sin
⁡
𝜃
−
𝑅
𝑟
sin
⁡
(
𝑅
𝑝
+
𝑅
𝑟
𝑅
𝑟
𝜃
)
This forms the outer flank of the tooth.

4.2 Hypocycloid (Dedendum)
Generated by rolling the circle inside the pitch circle.

Parametric form:

𝑥
(
𝜃
)
=
(
𝑅
𝑝
−
𝑅
𝑟
)
cos
⁡
𝜃
+
𝑅
𝑟
cos
⁡
(
𝑅
𝑝
−
𝑅
𝑟
𝑅
𝑟
𝜃
)
𝑦
(
𝜃
)
=
(
𝑅
𝑝
−
𝑅
𝑟
)
sin
⁡
𝜃
−
𝑅
𝑟
sin
⁡
(
𝑅
𝑝
−
𝑅
𝑟
𝑅
𝑟
𝜃
)
This forms the inner flank of the tooth.

In your implementation, both curves are sampled adaptively using:

Code
CBT_CycloidGear.getAddendumCurve()
CBT_CycloidGear.getDedendumCurve()
5. Addendum and Dedendum Heights
You use two dimensionless parameters:

hA — addendum height factor

hD — dedendum height factor

These scale the radial extent of the tooth:

𝑅
outer
=
𝑅
𝑝
+
ℎ
𝐴
⋅
𝑚
𝑅
root
=
𝑅
𝑝
−
ℎ
𝐷
⋅
𝑚
This is consistent with clockmaking practice, where:

ℎ
𝐴
≈
1.0

ℎ
𝐷
≈
1.05
 to 
1.25

Your defaults match the literature.

6. Fillet Geometry
Cycloidal gears naturally produce a hypocycloid cusp at the root.
This cusp is sharp and cannot be milled directly.

Your implementation replaces the cusp with a circular fillet of radius:

Code
gear.radiusFillet
This radius is computed from:

dedendum rolling circle

dedendum height

tooth count

The fillet ensures:

smooth milling

no stress concentration

compatibility with CNC endmills

This is one of the most important practical improvements over the pure mathematical curve.

7. Full Tooth Construction
A complete tooth consists of:

Left dedendum flank

Left fillet

Left addendum flank

Right addendum flank

Right fillet

Right dedendum flank

Your code assembles these into:

Code
gear.getFullGear()
gear.getFullGap()
The gap is used for:

Fusion knife‑body workflow

OBJ export

CNC milling offset curves

8. Meshing Between Pinion and Wheel
Cycloidal gears mesh when:

𝑁
𝑤
𝑁
𝑝
=
ratio
and the center distance is:

𝐶
=
𝑅
𝑝
,
𝑝
+
𝑅
𝑝
,
𝑤
Your implementation uses:

Code
CBT_CycloidGear.createFromPinion()
which ensures:

correct pitch radii

correct rolling radii

correct dedendum/addendum matching

correct tooth orientation

This is the correct way to generate a matched pair.

9. Offset Curves for Milling
Milling requires offsetting the tooth gap by:

𝑑
=
millDiameter
2
+
stockToLeave
Your milling code uses:

Code
CBT_GearMilling.createFinishingMillCurve()
CBT_GearMilling.createRoughingMillCurve()
These curves are:

adaptively sampled

mirrored

rotated

assembled into full circular patterns

This is the geometric foundation of your G‑code generation.

10. Summary
This toolkit implements the full geometry of cycloidal gears:

pitch circle

rolling circle

epicycloid addendum

hypocycloid dedendum

fillet replacement

full tooth assembly

matched pinion/wheel generation

offset curves for milling

It is mathematically correct, clockmaker‑compatible, and CNC‑ready.
